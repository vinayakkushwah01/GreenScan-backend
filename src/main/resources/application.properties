# Spring Application
spring.application.name=greenscan
server.port=8080
server.servlet.context-path=/api/v1

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ===============================
# 🔐 DATABASE CONFIGURATION
# ===============================
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DATABASE_USERNAME:postgres.yvtvwetbpdcjzobgjhsp}
spring.datasource.password=${DATABASE_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# ===============================
# 🔐 SUPABASE CONFIGURATION
# ===============================
supabase.project-ref=${SUPABASE_PROJECT_REF:yvtvwetbpdcjzobgjhsp}
supabase.bucket-name=${SUPABASE_BUCKET_NAME:GreenScan}
supabase.api-key=${SUPABASE_API_KEY:}

# ===============================
# 🔐 JWT CONFIGURATION
# ===============================
jwt.secret=${JWT_SECRET:}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# ===============================
# 🔐 CLOUDINARY CONFIGURATION
# ===============================
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dj0zbnssu}
cloudinary.api-key=${CLOUDINARY_API_KEY:636628957112957}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}
cloudinary.secure=true

# ===============================
# 🔐 MAIL CONFIGURATION
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:service.greenscan@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ===============================
# 🔐 AI SERVICE CONFIGURATION
# ===============================
ai_detection_api_key=${AI_DETECTION_API_KEY:}

# ===============================
# 🔐 ENCRYPTION CONFIGURATION
# ===============================
encryption.secret-key=${ENCRYPTION_SECRET_KEY:x8F2r9P0LmB5Qs1Z}

# ===============================
# OTHER CONFIGURATIONS
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.fail-on-unknown-properties=false
spring.jackson.default-property-inclusion=always

# ===============================
# GREEN COINS CONFIGURATION
# ===============================
green-coins.rates.plastic=2.0
green-coins.rates.metal=5.0
green-coins.rates.paper=1.5
green-coins.rates.glass=3.0
green-coins.rates.electronic=10.0
green-coins.redemption.min-amount=100
green-coins.redemption.max-amount=10000

# ===============================
# NOTIFICATION CONFIGURATION
# ===============================
notification.enabled=true
notification.providers.email.enabled=true
notification.providers.push.enabled=true
notification.providers.sms.enabled=false
